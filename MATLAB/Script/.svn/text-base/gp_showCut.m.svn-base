function gp_showCut(A, inEW, inUParams)
format compact;

% Get the default config
config = gp_getConfig();
config.dofast = true;

% Configure whether we should consider edge weights.
ew = 0;
if nargin >= 2, ew = inEW; end

% Configure whether we should use custom user parameters.
uparams = gp_getDefaultUserParams();
if nargin >= 3, uparams = inUParams; end

% Do the cut and save the time, cost, balance, and Partition.
[T,C,B,P] = run_gp(A, ew, uparams, config);
fprintf('  Exec Time: %f\n', T);
fprintf('   Cut Cost: %f\n', C);
fprintf('  Imbalance: %f\n', B);

% Visualize the cut.
cspy(P);

